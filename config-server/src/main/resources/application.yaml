spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yunjae62/vault-cloud-config-demo
          default-label: main
          search-paths:
            - config
server:
  port: 8888